// Enterprise AutoConfig. The first line is intentionally skipped by
// the AutoConfig parser; managed preferences belong on the lines below.
lockPref("enterprise.console.address", "https://stormkeeper.sandbox.moz-enterprise.com");

[DEFAULT]

["test_shellService.js"]
skip-if = [
  "os == 'win'", # Windows: setDefaultClient requires user confirmation.
]
run-if = [
  "os != 'mac'"
 ] #  Mac: test perma failing on CI. Bug 1954960.

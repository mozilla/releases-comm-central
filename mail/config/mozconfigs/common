ac_add_options --enable-project=comm/mail
# TODO remove this again once we support the new installer build path.
ac_add_options --enable-makefile-installer-build

. "$topsrcdir/build/mozconfig.common.override"

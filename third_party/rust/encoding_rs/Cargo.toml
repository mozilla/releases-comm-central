# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
#
# When uploading crates to the registry Cargo will automatically
# "normalize" Cargo.toml files for maximal compatibility
# with all versions of Cargo and also rewrite `path` dependencies
# to registry (e.g., crates.io) dependencies.
#
# If you are reading this file be aware that the original Cargo.toml
# will likely look very different (and much more reasonable).
# See Cargo.toml.orig for the original contents.

[package]
edition = "2024"
rust-version = "1.88"
name = "encoding_rs"
version = "0.8.35"
authors = ["Henri Sivonen <hsivonen@hsivonen.fi>"]
build = false
include = [
    "src/**/*.rs",
    "/data",
    "Cargo.toml",
    "COPYRIGHT",
    "LICENSE*",
    "README.md",
]
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false
description = "A Gecko-oriented implementation of the Encoding Standard"
homepage = "https://docs.rs/encoding_rs/"
documentation = "https://docs.rs/encoding_rs/"
readme = "README.md"
keywords = [
    "encoding",
    "web",
    "unicode",
    "charset",
]
categories = [
    "text-processing",
    "encoding",
    "web-programming",
    "internationalization",
]
license = "(Apache-2.0 OR MIT) AND BSD-3-Clause"
repository = "https://github.com/hsivonen/encoding_rs"

[features]
alloc = []
default = ["alloc"]
fast-big5-hanzi-encode = []
fast-gb-hanzi-encode = []
fast-hangul-encode = []
fast-hanja-encode = []
fast-kanji-encode = []
fast-legacy-encode = [
    "fast-hangul-encode",
    "fast-hanja-encode",
    "fast-kanji-encode",
    "fast-gb-hanzi-encode",
    "fast-big5-hanzi-encode",
]
less-slow-big5-hanzi-encode = []
less-slow-gb-hanzi-encode = []
less-slow-kanji-encode = []
simd-accel = [
    "any_all_workaround",
    "rustversion",
]
std = ["multiversion/std"]

[lib]
name = "encoding_rs"
path = "src/lib.rs"

[dependencies]
cfg-if = "1.0"
multiversion_no_op = "1.0.0"

[dependencies.any_all_workaround]
version = "0.1.0"
optional = true

[dependencies.rustversion]
version = "1.0.19"
optional = true

[dependencies.serde]
version = "1.0"
optional = true

[dev-dependencies]
bincode = "1.0"
serde_derive = "1.0"
serde_json = "1.0"

[target.'cfg(all(any(target_arch = "x86_64", target_arch = "x86"), not(all(target_feature = "avx2", target_feature = "bmi1"))))'.dependencies.multiversion]
version = "0.8.0"
default-features = false

[target.'cfg(any(target_arch = "x86_64", target_arch = "x86"))'.dependencies]
core_detect = "1"

[target.'cfg(any(target_arch = "x86_64", target_arch = "x86", target_arch = "aarch64", all(target_arch = "wasm32", target_feature = "simd128")))'.dependencies.simdutf8]
version = "0.1.5"
features = [
    "aarch64_neon",
    "public_imp",
]
default-features = false

[lints.rust.unexpected_cfgs]
level = "warn"
priority = 0
check-cfg = ["cfg(fuzzing)"]

[profile.release]
lto = true

---
yamllint:
    description: YAML linter
    include:
        - comm/
    extensions: ['yml', 'yaml']
    support-files:
        - '**/.yamllint'
        - 'tools/lint/yamllint_/**'
    type: external
    payload: commlint:lint_wrapper
    wraps: yamllint_:lint
